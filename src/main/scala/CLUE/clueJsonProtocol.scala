package CLUE

/*import geotrellis.vector.io.json.GeometryFormats.PointFormat
import spray.json.{DefaultJsonProtocol, JsArray, JsValue, RootJsonFormat}
import spray.json._

object clueJsonProtocol extends DefaultJsonProtocol {
  implicit val ClueFeatureFormat = jsonFormat16(buildingFeature)
  //implicit val PlanningFormat = jsonFormat3(planningFeature)

  implicit object RootCollectionFormat extends RootJsonFormat[rootCollection] {
    def read(value: JsValue) = rootCollection(value.convertTo[List[buildingFeature]])
    def write(f: rootCollection) = JsArray(f.toJson)
  }
}
*/

